window.uploadfile=(id)=>{
    document.getElementById(id).click();
}

window.printpreview=()=>{
    window.print();
}

window.mapInit = (ll) => {

    //var latlng = new google.maps.LatLng(ll.lat, ll.lng);

    //var option = {
    //    zoom: 14,
    //    center: latlng,
    //    mapTypeId: google.maps.MapTypeId.ROADMAP
    //};

    //map = new google.maps.Map(document.getElementById("mapp"), option);
 
}

